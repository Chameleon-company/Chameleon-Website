import React from 'react';
import { ColumnProps } from '../Column';
declare function getTotalByColumns(columns: React.ReactElement<ColumnProps> | React.ReactNode[]): {
    totalFlexGrow: number;
    totalWidth: number;
};
export default getTotalByColumns;
